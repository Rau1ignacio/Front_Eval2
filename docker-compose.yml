services:
  frontend:
    build: .
    container_name: frontend_eval2
    restart: always
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      DEBUG: "False"
      BACKEND_URL: "http://10.0.130.102:3000"
      SECRET_KEY: "clave_super_segura"
